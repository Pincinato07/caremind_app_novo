    org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -XX:HeapBaseMinAddress=4G -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
    android.useAndroidX=true
    android.enableJetifier=true
    # Configurações para resolver problemas com espaços no caminho
    org.gradle.daemon=true
    org.gradle.parallel=false
    org.gradle.configureondemand=false
    # Configuração específica para Windows com espaços no caminho
    org.gradle.caching=false
    # Configurações adicionais para resolver problemas de caminho no Windows
    org.gradle.workers.max=1
    android.enableR8.fullMode=false
    # Configuração específica para evitar problemas de caminho
    android.enableBuildCache=false
